const formats = {
  formats: {
    date: {
      format: 'D.M.YYYY',
    },
    number: {
      currency: {
        unit: '$'
      },
      percentage: {
        template: '%n %'
      }
    }
  }
};

export default formats;
